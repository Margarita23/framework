import { Application } from "./models/application";
// const view = new SomeView();
// const controller = new SomeController(view);
let app = Application.instance;

export let application = Application.instance;
// app.run(view);